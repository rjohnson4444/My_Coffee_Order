<h1> <%= current_user.username %>'s Order's  </h1>

<% @orders.each do |order| %>

<%= link_to "Order number:#{order.id}", order_path(order) %>

<ul>
<li> Order quantity: <%= order.quantity %></li>
<li> Order total: <%= number_to_currency(order.sum) %></li>

</ul>

<%end %>
