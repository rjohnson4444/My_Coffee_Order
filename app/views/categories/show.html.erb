<head>
  <nav>
    <div class="nav-wrapper #000000 black">
      <a href="#" class="brand-logo ">My Coffee Now</a>
      <ul id="nav-mobile" class="right hide-on-med-and-down #40c4ff light-blue accent-2">
        <li><a href="sass.html">Login</a></li>
        <li><a href="badges.html">Menu</a></li>
        <li><a href="collapsible.html">Cart</a></li>
      </ul>
    </div>
  </nav>

  <table class="container">
    <thead>
      <tr>
        <th data-field="name">Item Name</th>
        <th data-field="price"> Price</th>
      </tr>
      <h1 class="container">Category-<%="#{@category.name}" %></h1>
      <% @category.items.each do |item| %>
      </thead>
      <tbody>
        <tr>
          <td><%= item.title %></td>
          <td> <%=number_to_currency(item.price) %></td>
          <% end %>
        </tbody>
